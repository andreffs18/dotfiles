############
### UTIS ###
############
# clear screen
alias c="clear"
# search and look information in directori
alias list="ls"
alias ls="list -lhFAG"
# ip information
alias ip='echo "Your IP IS:"; ifconfig | grep "inet " | grep -v 127.0.0.1'
# reload every dotfile
alias resource='echo "Source ~/.zshrc"; source ~/.zshrc; echo "Success!";'
# project root folder localtion
alias desktop="c && cd ~/Desktop/"
alias documents="c && cd ~/Documents/"
alias projects="c && ~/projects/"
alias work="c && ~/work/"
# clear all .pyc files from current directory
alias delpyc="find . -name '*.pyc' -delete"
# update dotfiles
alias updotfiles='function _updotfiles(){
    read -r -p "Update DOT Files? [y/N] " response
    if [[ $response =~ ^([yY][eE][sS]|[yY])$ ]]
    then
        dotfiledir=~/projects/dotfiles/
        # copy files to dotfile folder and commit code
        cp ~/.{bash_profile,bashrc,gitignore,mansettings,myalias,profile,promptsettings} $dotfiledir
        # update github
        cd $dotfiledir
        gitcp "UPDATE DOT FILES"
    else
        echo "What do we say to the God of Death? Not today."
    fi
};_updotfiles'

# grep string on given folder
alias findstr='function _findstr(){
    find $1 \( -name node_modules -prune \) -o -name "*.py" -exec grep --color -Hn "$2" {} 2>/dev/null \;
};_findstr'

##############################
### APPLICATIONS SHORTCUTS ###
##############################
alias st2='open $1 -a Sublime\ Text\ 2'

######################
### PORTFOLIO UTIS ###
######################
# virtual enviroments
alias _vm_andreffs="workon andreffs"
# goto projects folder
alias _andreffsfolder="projects && cd andreffs/"
# start my server
alias andreffs="_andreffsfolder && _vm_andreffs && gits"

####################
### COLLEGE UTIS ###
####################
# current year i'm @
alias IST="cd ~/Dropbox/IST"

################
### GIT UTIS ###
################
alias gits="git status"
alias gitd="c && git diff"
alias gitcp='function _gitcp(){
	current_branch=$(git symbolic-ref HEAD --short)
	echo "Commit Message: \"$1\"";
	echo "origin->$current_branch";
	git commit -am "$1";
	git push origin $current_branch;
	echo "Success!";
};_gitcp'
alias gitrmerged='git branch --merged | grep -v "\*" | grep -v release | grep -v master | xargs -n 1 git branch -d'

####################
### UNBABEL UTIS ###
####################
# virtual enviroments
alias _vm_u="workon u"
alias _vm_uannotation="workon uannotation"
alias _vm_uops="workon uops"

# handle mongodb
alias startmdb="echo '>>> Starting MONGODB SERVER' && mongod --dbpath /usr/local/var/mongodb/"
alias stopmdb="ps aux | grep mongod | awk {'print \$2'} | xargs kill -9"
# start redis_server (background)
alias startredis="echo '>>> Starting REDIS SERVER' && redis-server &"
# start smtp server
alias startsmtp="echo '>>> Starting SMTP SERVER' && python -m smtpd -n -c DebuggingServer localhost:1025"
# start notebooks
alias inb="echo '>>> Starting iPYTHON NOTEBOOK' && ipython notebook"
# goto projects folder
alias _ufolder="work && cd unbabel/ && _vm_u"
alias _uannotationfolder="work && cd error-check/ && _vm_uannotation"
alias _uopsfolder="work && cd unbabel-ops/ && _vm_uops"
# run projects
alias u="_ufolder && gits"
alias uberrorcheck="_uannotationfolder && gits"
alias ubops="_uopsfolder && cd playbooks && git pull && gits"
# python manage.py projects
alias py="python manage.py runserver"